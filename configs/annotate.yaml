input: data/clustered_datasets/test  # Can be single FASTA file or folder with multiple FASTA files
k: 1 # number of nearest neighbours
id_to_annotation: data/cath-domain-sf-list.txt # id to annotation file, to map hits to CATH SF's
model_path: /SAN/orengolab/functional-families/contrasted/outputs/2025-10-23/17-47-07/checkpoints/epoch=epoch=100-val_knn_bal_acc=val/knn_balanced_accuracy=0.9371.ckpt  # trained model checkpoint
embedding_file: data/cath-domain-seqs-S100.h5  # HDF5 file with pre-computed embeddings
ids: null  # the ids pertaining to the entries of our vector index (loaded automatically from .npy)
index: data/vector_db/train.index  # faiss vector index file
output_dir: outputs/annotations  # directory where annotations will be saved
distance_cutoff: 1  # maximum cosine distance (1 - similarity) to consider valid
return_distance: true
return_confidence: true
return_true_annotation: true  # include true annotations and compute metrics