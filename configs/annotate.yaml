input: ???
method: centroid
k: 1
id_to_annotation: null
# Resolved against $CONTRASTED_DATA_DIR (default ~/.cache/contrasted).
model_path: aa3di_s40_seed40_head.pt
embedding_dir: null
index: cath_s40_centroids.pt
output_dir: outputs/annotations

# Val-locked 99% precision gate for the canonical S40 head (seed 40).
# Recalibrate if you swap the head.
distance_cutoff: 0.24699
return_distance: true
return_confidence: false
return_true_annotation: false
batch_size: 8192
tm_align: false
structure_dir: null
tmalign_binary: TMalign
compute_metrics: false
num_thresholds: 50
