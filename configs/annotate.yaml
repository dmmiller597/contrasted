input: ???
method: centroid
k: 1
id_to_annotation: null
# Resolved against $CONTRASTED_DATA_DIR (default ~/.cache/contrasted).
model_path: aa3di_s20_seed40_head.pt
embedding_dir: null
index: cath_s20_centroids.pt
output_dir: outputs/annotations

# Cutoff for the published S20 head. Override if you train your own.
distance_cutoff: 0.24048042
return_distance: true
return_confidence: false
return_true_annotation: false
batch_size: 8192
tm_align: false
structure_dir: null
tmalign_binary: TMalign
compute_metrics: false
num_thresholds: 50
